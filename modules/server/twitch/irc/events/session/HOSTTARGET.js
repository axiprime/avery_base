export const name = 'HOSTTARGET';
export const execute = async (client, command, channel, hostingChannel, numberOfViewers) => {
    if (client.irc_Debug[command])
        console.info(`[${client.utils.getLocalDate()}][HOSTTARGET] : `,args)

}